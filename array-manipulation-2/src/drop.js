/* exported drop */

function drop(array, count) {
  return array.slice(count);
}

// my google search terms were:
// mdn js array method subset
// unsure if this goes against the point of the exercise
// but it seemed more realistic than a weird conditional wrapped for loop
